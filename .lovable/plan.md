

## Canhoto montagem — texto em maiúsculas

### Alteração

**Arquivo**: `src/pages/ReportsPage.tsx` (linhas 642-644)

Aplicar `.toUpperCase()` nos textos `stub3Line1` e `stub3Line2` ao renderizar:

- Linha 642: `doc.text(stub3Line1.toUpperCase(), ...)`
- Linha 644: `doc.text(stub3Line2.toUpperCase(), ...)`

